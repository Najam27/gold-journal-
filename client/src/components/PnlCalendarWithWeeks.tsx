import React, { useMemo, useState } from "react";
import { ChevronLeft, ChevronRight, CalendarDays } from "lucide-react";
import { Button } from "@/components/ui/button";
import { formatMoney, toNumber } from "@/lib/gold";
import { getPktDateKey, getPktMonthKey, pktMonthCalendar, addPktMonths, formatPktMonth } from "@shared/pktDate";
import { MonthlyTradeOverview } from "@/components/MonthlyTradeOverview";

const weekdays = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
const displayDay = (key: string) => new Intl.DateTimeFormat("en-GB", { timeZone: "Asia/Karachi", day: "2-digit", month: "short" }).format(new Date(`${key}T12:00:00+05:00`));

export function PnlCalendarWithWeeks({ trades }: { trades: any[] }) {
  const [selectedMonth, setSelectedMonth] = useState(() => getPktMonthKey()); const [selectedDay, setSelectedDay] = useState<string | null>(null); const calendar = useMemo(() => pktMonthCalendar(selectedMonth), [selectedMonth]);
  const cells = useMemo(() => [...Array.from({ length: calendar.firstWeekday }, () => null), ...calendar.days], [calendar]);
  const weeks = useMemo(() => Array.from({ length: Math.ceil(cells.length / 7) }, (_, index) => cells.slice(index * 7, index * 7 + 7)), [cells]);
  const moveMonth = (offset: number) => setSelectedMonth(current => addPktMonths(current, offset));
  return <><MonthlyTradeOverview trades={trades} selectedMonth={selectedMonth} onMonthChange={setSelectedMonth} /><section className="section-heading calendar-heading"><div><span className="eyebrow">P&L RHYTHM</span><h2>{formatPktMonth(selectedMonth)}</h2><p>Hover, focus, or tap a populated day to bring the P&L to the center. Each weekly card keeps the total visible before the next session.</p></div><div className="calendar-nav"><Button variant="outline" aria-label="Previous month" onClick={() => moveMonth(-1)}><ChevronLeft size={17} /> Previous month</Button><Button variant="outline" onClick={() => setSelectedMonth(getPktMonthKey())}>Today</Button><Button variant="outline" aria-label="Next month" onClick={() => moveMonth(1)}>Next month <ChevronRight size={17} /></Button></div></section><section className="panel pnl-week-calendar"><div className="pnl-week-grid-header"><span className="pnl-week-spacer" />{weekdays.map(day => <span key={day}>{day}</span>)}</div><div className="pnl-week-rows">{weeks.map((week, weekIndex) => { const present = week.filter((day): day is string => Boolean(day)); const start = present[0] ?? `${selectedMonth}-01`; const end = present.at(-1) ?? start; const rows = trades.filter(trade => { const key = getPktDateKey(trade.tradeDate); return key >= start && key <= end; }); const pnl = rows.reduce((sum, trade) => sum + toNumber(trade.pnl), 0); const weeklyRisk = rows.reduce((sum, trade) => sum + Math.abs(toNumber(trade.risk)), 0); const ratio = weeklyRisk ? pnl / weeklyRisk * 100 : 0; const weekTone = pnl > 0 ? "profit" : pnl < 0 ? "loss" : "flat"; return <div className="pnl-week-row" key={`${selectedMonth}-${weekIndex}`}><aside className={`week-summary-card ${weekTone}`}><span>Week {weekIndex + 1} · {ratio >= 0 ? "+" : ""}{ratio.toFixed(2)}%</span><strong className="data-text">{formatMoney(pnl)}</strong><small>{rows.length} trade{rows.length === 1 ? "" : "s"} · ending {displayDay(end)}</small></aside><div className="pnl-day-grid">{week.map((day, index) => { if (!day) return <div className="pnl-day-card muted-day" key={`blank-${index}`} aria-hidden="true" />; const dayTrades = trades.filter(trade => getPktDateKey(trade.tradeDate) === day); const daily = dayTrades.reduce((sum, trade) => sum + toNumber(trade.pnl), 0); const wins = dayTrades.filter(trade => trade.result === "WIN").length; const winRate = dayTrades.length ? wins / dayTrades.length * 100 : 0; const populated = dayTrades.length > 0; const selected = populated && selectedDay === day; return <button type="button" className={`pnl-day-card ${populated ? daily >= 0 ? "gain" : "loss" : "empty"} ${populated ? "populated" : ""} ${selected ? "selected" : ""}`} key={day} aria-label={populated ? `${displayDay(day)}: ${formatMoney(daily)}, ${dayTrades.length} trades` : `${displayDay(day)}: no trades`} aria-pressed={populated ? selected : undefined} disabled={!populated} onClick={() => populated && setSelectedDay(selected ? null : day)}><span className="pnl-day-number">{day.slice(-2)}</span>{populated && <span className="pnl-day-main"><strong className="data-text">{formatMoney(daily)}</strong><small>{dayTrades.length} trade{dayTrades.length === 1 ? "" : "s"} · {winRate.toFixed(0)}% win</small></span>}</button>; })}</div></div>; })}</div></section><div className="calendar-touch-note"><CalendarDays size={14} /> Tap a populated card or use keyboard focus to emphasize its result. Daily data remains private to the active account.</div></>;
}
